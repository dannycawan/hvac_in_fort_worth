package com.hvac.fortworth

import io.flutter.embedding.android.FlutterActivity

class MainActivity : FlutterActivity() {
    // Tidak perlu menambahkan kode lain
    // Semua logika Ads (Banner, Interstitial, Native, App Open) sudah di-handle di Dart (main.dart)
}
